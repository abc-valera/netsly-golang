openapi: 3.0.0
info:
  version: 0.1.0
  title: Netsly API
  termsOfService: "https://github.com/abc-valera/netsly-api-golang"
  contact:
    email: zoocity14@gmail.com
    url: "https://github.com/abc-valera/netsly-api-golang"
  # x-logo:
  #   url: "https://apis.guru/openapi-template/logo.png"
  description: API for Netsly social network
externalDocs:
  description: Learn more about the project.
  url: "https://github.com/abc-valera/netsly-api-golang"
tags:
  - name: sign
    description: Everything about managing user sessions
  - name: users
    description: Everything about searching users
  - name: jokes
    description: Everything about searching jokes
  - name: comments
    description: Everything about searching comments
  - name: likes
    description: Everything about counting likes
  - name: me
    description: Everything about managing current user profile
servers:
  - url: "http://localhost:3000"
paths:
  /sign_up:
    $ref: paths/sign/sign-up.yaml
  /sign_in:
    $ref: paths/sign/sign-in.yaml
  /sign_refresh:
    $ref: paths/sign/sign-refresh.yaml
  /me:
    $ref: paths/me/me.yaml
  /me/jokes:
    $ref: paths/me/me_jokes.yaml
  /me/comments:
    $ref: paths/me/me_comments.yaml
  /me/likes:
    $ref: paths/me/me_likes.yaml
  /comments/{joke_id}:
    $ref: paths/comments/comments_{joke_id}.yaml
  /likes/{joke_id}:
    $ref: paths/likes/likes_{joke_id}.yaml
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
