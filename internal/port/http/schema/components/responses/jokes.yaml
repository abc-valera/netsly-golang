description: Array of jokes
content:
  application/json:
    schema:
      type: object
      properties:
        jokes:
          type: array
          items:
            $ref: "../schemas/joke.yaml"
      required:
        - jokes
