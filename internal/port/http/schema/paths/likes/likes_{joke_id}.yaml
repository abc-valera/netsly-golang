get:
  tags:
    - likes
  summary: Counts likes of the joke
  operationId: LikesByJokeIDGet
  parameters:
    - name: joke_id
      in: path
      description: ID of the joke to count likes
      required: true
      schema:
        type: string
  responses:
    "200":
      description: Count of likes
      content:
        application/json:
          schema:
            type: integer
            minimum: 0
    default:
      $ref: "../../components/responses/code_error.yaml"
