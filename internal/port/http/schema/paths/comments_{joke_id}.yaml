get:
  tags:
    - comments
  description: Returns comments of the joke
  parameters:
    - name: joke_id
      in: path
      description: ID of the joke to get comments
      required: true
      schema:
        type: string
  security:
    - bearerAuth: []
  responses:
    "200":
      $ref: "../components/responses/comments.yaml"
    default:
      $ref: "../components/responses/code_error.yaml"
