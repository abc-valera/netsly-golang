description: List of room messages
content:
  application/json:
    schema:
      type: object
      properties:
        room_messages:
          type: array
          items:
            $ref: "../schemas/room_message.yaml"
      required:
        - room_messages
